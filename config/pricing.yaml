# Provider and model pricing configuration
#
# This file will contain pricing information for LLM providers and models
# in a later phase. Format and structure to be defined when implementing
# budget management.
#
# DO NOT manually add current provider pricing - this will be populated
# through the appropriate phase implementation.
# Example pricing configuration used for development and unit tests.
# Values are illustrative only and should not be treated as current provider pricing.
providers:
  groq:
    llama-3.3-70b-versatile:
      quality_tier: "smart"
      max_tokens: 8192
      cost_per_input_token: "0.000005"
      cost_per_output_token: "0.000015"

  gemini:
    gemini-2.5-flash:
      quality_tier: "fast"
      max_tokens: 8192
      cost_per_input_token: "0.0000015"
      cost_per_output_token: "0.0000060"

  openrouter:
    openrouter-free:
      quality_tier: "fast"
      max_tokens: 4096
      cost_per_input_token: "0.0000015"
      cost_per_output_token: "0.0000060"